import { Container } from "react-bootstrap";

export const MainNoticeAlert = () => {

    return(
        <div className="MainNoticeAlert">
          <Container>
            <p>You will be automatically entered into the monthly draw every 3 mints!!!</p>
          </Container>
        </div>
    
    );
}